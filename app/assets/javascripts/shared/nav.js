$(function(){
  $('#toggle-left').click(function(){
    //$('#main-wrapper').toggleClass('sidebar-mini');
    // need to add icons
    // should also add styling to collapse the upper left site icon
  });
});
